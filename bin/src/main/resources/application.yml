
jwt:
  secret: '[a-zA-Z0-9._]^+$Guidelines89797987forAlphabeticalArraNumeralsandOtherSymbo$'

#  error:
#    whitelabel:
#      enabled: false

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hale_db #change this to your database url
    password: Cotemar2021 #change this to your database password 
    username: GAJUAREZDELAC #change this to your database username
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  web:
    resources:
      add-mappings: false
  
 